#db??
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/grepp_sec_form
    username: happy
    password: jieun

  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: create
#spring secutiry에 해당하는 모든 로그를 볼 수 있음
logging:
  level:
    org.springframework.security: TRACE